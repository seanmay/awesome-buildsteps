{
  "name": "awesome-buildsteps",
  "version": "0.0.1",
  "main": "dist/index.js",
  "devDependencies": {
    "@types/react": "^16.3.17",
    "typescript": "^2.9.1"
  },
  "dependencies": {
    "react": "^16.4.0"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "dev": "cd example && npx parcel src/index.html",
    "install-dependencies": "cd example && yarn install",
    "postinstall-dependencies": "echo 'YOU HAVE, ALSO.'",
    "postinstall": "echo 'I HAVE INSTALLED' && yarn install-dependencies"
  }
}
